<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light">
        <!-- Day / Light Mode  theme here. -->
        <item name="colorPrimary">@color/whiteSmoke</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="backgroundcolor">#eeeeee</item>
        <item name="cardBackground">@color/babyblue</item>
        <item name="textcolor">#090909</item>
        <item name="tintcolor">#80808080</item>
        <item name="buttoncolor">@color/azure</item>

        <item name="buttoncolortint">@color/babyblue</item>
        <item name="boardcolor">@color/azure</item>

        <item name="android:windowAnimationStyle">@style/WindowAnimationTransition</item>

    </style>

    <!-- Dark / Night Mode Application theme. -->
    <style name="DarkTheme" parent="ThemeOverlay.AppCompat.Dark">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/mahogany</item>
        <item name="colorPrimaryDark">@color/space</item>
        <item name="colorAccent">@color/azure</item>
        <item name="backgroundcolor">@color/mahogany</item>
        <item name="cardBackground">@color/space</item>
        <item name="textcolor">#ffffff</item>
        <item name="tintcolor">#eeeeee</item>
        <item name="buttoncolor">@color/azure</item>
        <item name="boardcolor">@color/azure</item>
        <item name="buttoncolortint">@color/babyblue</item>
        <item name="android:windowAnimationStyle">@style/WindowAnimationTransition</item>

    </style>

    <!-- Tranistion Animation Fade In/Out -->
    
    <style name="WindowAnimationTransition">
        <item name="android:windowEnterAnimation">@android:anim/fade_in</item>
        <item name="android:windowExitAnimation">@android:anim/fade_out</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="DialogAnimation_up_down">
        <item name="android:windowEnterAnimation">@anim/slide_up</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowExitAnimation">@anim/slide_bottom</item>

    </style>

</resources>